import {
  runAnimation,
  runColorTheme,
  runExecBrowserFunction,
  runFetchJSON,
  runFillWithImageUrl,
} from "./demos";

// runAnimation();
// runColorTheme();
// runExecBrowserFunction();
// runFetchJSON();
// runFillWithImageUrl();

// eval string as js.

// plugin + REST ui builder idea
